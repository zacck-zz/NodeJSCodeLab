//iterator
var i = 0;
var countLimit = 9;

//while loop
while(i < countLimit) {
  //lets log to screen
  console.log('while', i);
  //lets increment the i
  i++;
}


//for loop
for(i = 0; i < countLimit; i++) {
  //lets log on every iteration
  console.log('for', i);
}
